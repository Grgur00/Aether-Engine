# Benchmarking

Every experiment starts with a falsifiable hypothesis and baseline. Record warmup, measurement,
forks, profilers, seed, git state, Java, OS, CPU, memory, filesystem, and exact command. Retain raw
JSON results. Hosted CI runners may run smoke benchmarks but their numbers must not support
performance claims.
