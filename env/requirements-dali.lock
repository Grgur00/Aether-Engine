# Optional direct dependency pin for the canonical DALI campaign on Kaggle.
# Install after requirements.lock; capture the resolved transitive set with pip freeze.
# CUDA runtime support and GPU validation remain required.
nvidia-dali-cuda120==2.3.0; platform_system == "Linux" and platform_machine == "x86_64"
